# Redmine Java API Tests

Some tests with the [taskadapter/redmine-java-api](https://github.com/taskadapter/redmine-java-api) lib.

Run `$ docker-compose up` to get a running instance of redmine using docker.

